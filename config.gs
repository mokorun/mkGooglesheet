
/* 共通設定 */
function getConf(){
  
  //テンプレスプレッドシートのID

  //GWS
  var key = "**";

  //LOG
  var logkey = "**";
  
  //chatworkのトークン
  var token = "**";

  //出力フォルダ

  //GWS
  var setFold_a = "**";
  var setFold_b = "**";
  var setFold_c = "**";
  
  //返事するルームID
  var room_id = "**"; //A-BSE
  
  return {
    key : key,
    logkey : logkey,
    token : token,
    setFold_a : setFold_a,
    setFold_b : setFold_b,
    setFold_c : setFold_c,
    room_id : room_id
  }  
}
